language: php

php:
  - 5.5
  - 5.6
  - 7.0
  - hhvm
  - hhvm-nightly

matrix:
  allow_failures:
    - php: 7.0
    - php: hhvm-nightly
  fast_finish: true

install:
  - composer install
  - rsync -av tests/fixtures/my-project/ /tmp/my-project/
  - cd /tmp/my-project/; composer install; cd -

script: phpunit --boot=vendor/autoload.php tests/
notifications: { email: false }
